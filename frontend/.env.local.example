# Copy this file to .env.local and fill in your values.
#
# LOCAL DEV: keep this as-is (backend runs on localhost:8000)
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# PRODUCTION: replace with your Cloud Run URL after running deploy.sh
# NEXT_PUBLIC_BACKEND_URL=https://storybloom-xxxx-uc.a.run.app
